# Single-node Kafka (KRaft, no ZooKeeper) + Kafka UI for the T24 CALLJ -> Kafka demo
#
#   docker compose up -d                          Kafka on localhost:9092, UI on http://localhost:8080
#   KAFKA_HOST=192.168.1.10 docker compose up -d  when T24 runs on another machine
#                                                 (Windows CMD: set KAFKA_HOST=192.168.1.10)
#
# KAFKA_HOST must be the address the T24 server uses to reach this machine: Kafka returns it to
# clients after the first connection (advertised listener).
services:
  kafka:
    image: apache/kafka:3.7.1
    container_name: t24-demo-kafka
    ports:
      - "9092:9092"
    environment:
      KAFKA_NODE_ID: 1
      KAFKA_PROCESS_ROLES: broker,controller
      KAFKA_LISTENERS: INTERNAL://:19092,EXTERNAL://:9092,CONTROLLER://:9093
      KAFKA_ADVERTISED_LISTENERS: INTERNAL://kafka:19092,EXTERNAL://${KAFKA_HOST:-localhost}:9092
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: INTERNAL:PLAINTEXT,EXTERNAL:PLAINTEXT,CONTROLLER:PLAINTEXT
      KAFKA_INTER_BROKER_LISTENER_NAME: INTERNAL
      KAFKA_CONTROLLER_LISTENER_NAMES: CONTROLLER
      KAFKA_CONTROLLER_QUORUM_VOTERS: 1@kafka:9093
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: 1
      KAFKA_GROUP_INITIAL_REBALANCE_DELAY_MS: 0
      KAFKA_NUM_PARTITIONS: 3
      KAFKA_AUTO_CREATE_TOPICS_ENABLE: "true"

  kafka-ui:
    image: provectuslabs/kafka-ui:latest
    container_name: t24-demo-kafka-ui
    depends_on:
      - kafka
    ports:
      - "8080:8080"
    environment:
      KAFKA_CLUSTERS_0_NAME: t24-demo
      KAFKA_CLUSTERS_0_BOOTSTRAPSERVERS: kafka:19092
